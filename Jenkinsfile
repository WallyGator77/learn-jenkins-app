pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World - no shell'
                sh 'echo "Hello from Jenkins - shell"'
            }
        }
    }
}
